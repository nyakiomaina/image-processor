# Rust Image Processing Application

The project leverages the `image` crate for basic image manipulation tasks and the `rayon` crate to parallelize operations.

## Features

- Load images.
- Convert images to grayscale.
- Brighten images.
- Save processed images.
  
## Setup

Clone this repo

```bash
cd image_processor
cargo run
